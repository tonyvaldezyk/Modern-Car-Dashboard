const path = require('path');
const nodeExternals = require('webpack-node-externals');

module.exports = {
  mode: 'production', // 或 'production'
  entry: './src/index.js', // 调整为您的入口文件
  output: {
    filename: 'abandonware-noble.js',
    path: path.resolve(__dirname, 'dist'),
  },
  target: 'node', // 设置目标环境为 Node.js
  externals: [nodeExternals()], // 排除 Node.js 核心模块和外部依赖
  module: {
    rules: [
      {
        test: /\.js$/,
        exclude: /node_modules/,
        use: {
          loader: 'babel-loader',
          options: {
            presets: ['@babel/preset-env'],
          },
        },
      },
    ],
  },
  resolve: {
    extensions: ['.js']
  },
};
